# v5.5.2.4 · Autosave de tarjetas

- Las opciones de Diseño > Tarjetas se guardan automáticamente al mover cada control.
- Se mantienen las trazas `[Kiosko Settings]` para verificar el PATCH.
- Esto evita depender del botón Guardar del modal para estos controles.
- El cambio se aplica en vivo a las tarjetas y se persiste en `settings.json`.
