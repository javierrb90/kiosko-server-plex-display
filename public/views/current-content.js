function escapeHtml(value) { return String(value ?? '').replace(/[&<>'\"]/g, c => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', "'":'&#39;', '"':'&quot;' }[c])); }
function escapeAttr(value) { return escapeHtml(value).replace(/`/g, '&#96;'); }
function text(value) { return Array.isArray(value) ? value.filter(Boolean).join(' · ') : (value ? String(value) : ''); }
function setText(el, value) { const formatted = text(value); el.textContent = formatted || ''; el.hidden = !formatted; }
function slug(value) { return String(value || '').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '') || 'item'; }
function canonicalFor(media = {}) {
  if (media.canonicalId) return media.canonicalId;
  if (media.source === 'playnite' || media.kind === 'game') return `playnite:${slug(media.gameId || media.title)}`;
  return `plex:${media.collectionType || media.type || 'item'}:${media.ratingKey || slug(media.title)}`;
}
function stars(value = 0) { const n = Math.max(0, Math.min(5, Number(value) || 0)); return `<span class="star-rating" aria-label="${n} de 5">${'★'.repeat(n)}${'☆'.repeat(5 - n)}</span>`; }
function normalizeContent(input = {}) {
  if (!input) return null;
  const source = input.source || (input.kind === 'game' ? 'playnite' : input.kind === 'plex' ? 'plex' : input.platforms ? 'playnite' : 'plex');
  const isGame = source === 'playnite' || input.kind === 'game' || input.platforms;
  if (isGame) return { ...input, kind: 'game', source: 'playnite', collectionType: 'games', label: 'JUGANDO AHORA', title: input.title || 'Juego sin título', subtitle: text(input.platforms), poster: input.cover || input.poster || input.posterUrl || input.coverPath || null, backdrop: input.background || input.backdrop || input.backdropUrl || input.backdropPath || null, lines: [['Desarrollador', text(input.developers)], ['Distribuidor', text(input.publishers)], ['Lanzamiento', input.releaseYear], ['Géneros', text(input.genres)]].filter(([, v]) => Boolean(v)) };
  return { ...input, kind: 'plex', source: 'plex', collectionType: input.collectionType || (input.type === 'movie' ? 'movies' : 'series'), label: ({ play: 'REPRODUCIENDO', pause: 'PAUSADO', stop: 'DETENIDO', recently_added: 'AÑADIDO' }[input.event] || 'PLEX'), title: input.title || 'Sin reproducción', subtitle: input.subtitle || '', poster: input.posterUrl || input.poster || input.cover || null, backdrop: input.backdropUrl || input.backdrop || input.background || null, lines: [['Año', input.year], ['Tipo', input.type]].filter(([, v]) => Boolean(v)) };
}

export function createCurrentContentView({ api, ui, controlsRoot } = {}) {
  let el; let current = null; let isVisible = false; let onDeckMap = {}; let ratings = {}; let settings = {};
  function statusMarkup(media) {
    if (!media) return '';
    const key = canonicalFor(media);
    if (onDeckMap?.[key]) return `<span class="deck-pill">On Deck</span>`;
    const rating = ratings?.[key]?.rating || 0;
    return rating ? stars(rating) : '';
  }
  async function clearCurrent() { await api('/api/current/clear', { method: 'POST' }); current = null; render(null); ui.toast('Contenido actual limpiado'); }
  async function addToDeck() { await api('/api/current/deck', { method: 'POST' }); ui.toast('Añadido a On Deck'); }
  async function askRating(item) { return ui.open({ title: item.source === 'plex' ? 'Marcar como visto' : 'Marcar como terminado', className: 'ui-modal-root--rating', body: `<div class="rating-modal"><div class="rating-modal__poster">${item.poster ? `<img src="${escapeAttr(item.poster)}" alt="">` : ''}</div><div class="rating-modal__copy"><h3>${escapeHtml(item.title || 'Sin título')}</h3><p>${escapeHtml(item.subtitle || '')}</p><fieldset class="rating-picker">${[1,2,3,4,5].map(n => `<button type="button" data-rating="${n}">☆</button>`).join('')}</fieldset></div></div>`, actions: [{ label: 'Cancelar', value: null }, { label: 'Confirmar', variant: 'primary', onClick: root => Number(root.querySelector('.rating-picker')?.dataset.value || 0) }] }); }
  async function completeCurrent() { if (!current) return; const rating = await askRating(current); if (rating === null) return; await api('/api/current/complete', { method: 'POST', body: JSON.stringify({ rating }) }); ui.toast(current.source === 'plex' ? 'Marcado como visto' : 'Marcado como terminado'); }
  function openActions() { if (!current) return; ui.actionSheet({ title: current?.title || 'Contenido actual', actions: [ { id: 'deck', label: 'Añadir a On Deck', description: 'Pasar a lo que estás siguiendo ahora', disabled: Boolean(onDeckMap?.[canonicalFor(current)]), run: addToDeck }, { id: 'complete', label: current.source === 'plex' ? 'Marcar como visto' : 'Marcar como terminado', description: 'Valorar y mover a Colecciones', run: completeCurrent }, { id: 'clear', label: 'Limpiar contenido actual', description: 'Dejar la vista Actual vacía', run: clearCurrent } ] }); }
  function renderControls() { if (!controlsRoot || !isVisible) return; controlsRoot.innerHTML = `<button class="topbar-button view-actions-button" type="button" data-current-actions aria-label="Acciones de contenido actual">•••</button>`; }
  function render(data = current) {
    const media = normalizeContent(data); current = media; if (!el) return; const root = el.querySelector('.media-view'); const empty = el.querySelector('.current-empty'); root.classList.toggle('media-view--empty', !media); empty.hidden = Boolean(media);
    if (!media) { el.querySelector('.media-title').textContent = ''; el.querySelector('.media-poster').removeAttribute('src'); el.querySelector('.media-bg-img').removeAttribute('src'); return; }
    root.dataset.source = media.source || 'current'; el.querySelector('.media-event').textContent = media.label || 'ACTUAL'; el.querySelector('.media-title').textContent = media.title || 'Sin título'; setText(el.querySelector('.media-subtitle'), media.subtitle); el.querySelector('.current-status').innerHTML = statusMarkup(media);
    const meta = el.querySelector('.media-meta-lines'); meta.innerHTML = (media.lines || []).slice(0, 5).map(([label, value]) => `<p><strong>${escapeHtml(label)}:</strong> ${escapeHtml(value)}</p>`).join(''); const poster = el.querySelector('.media-poster'); poster.src = media.poster || ''; poster.style.visibility = media.poster ? 'visible' : 'hidden'; const bg = el.querySelector('.media-bg-img'); bg.src = media.backdrop || media.poster || ''; bg.classList.toggle('media-bg-img--visible', Boolean(media.backdrop || media.poster));
  }
  return { id: 'current-content', mount(target) { el = target; el.innerHTML = `<div class="media-view media-view--current"><img class="media-bg-img" alt=""><div class="media-bg-treatment"></div><div class="media-overlay"></div><div class="current-empty" hidden><h1>Actual <span>0</span></h1><p>No hay nada en reproducción. Cuando empieces a ver algo en Plex o lances un juego desde Playnite aparecerá aquí.</p></div><div class="media-layout"><div class="item-visual item-visual--plain"><img class="media-poster" alt="Carátula"></div><div class="media-info"><span class="media-event"></span><h1 class="media-title"></h1><p class="media-subtitle"></p><div class="current-status"></div><div class="media-meta-lines"></div></div></div></div>`; controlsRoot?.addEventListener('click', event => { if (isVisible && event.target.closest('[data-current-actions]')) openActions(); }); document.addEventListener('click', event => { const btn = event.target.closest('.rating-picker button'); if (!btn) return; const picker = btn.closest('.rating-picker'); picker.dataset.value = btn.dataset.rating; picker.querySelectorAll('button').forEach(node => { node.textContent = Number(node.dataset.rating) <= Number(btn.dataset.rating) ? '★' : '☆'; }); }); render(null); }, show() { isVisible = true; el.classList.add('view--active'); el.setAttribute('aria-hidden', 'false'); renderControls(); render(); }, hide() { isVisible = false; el.classList.remove('view--active'); el.setAttribute('aria-hidden', 'true'); if (controlsRoot) controlsRoot.innerHTML = ''; }, update(data = {}) { if (data?.currentContent !== undefined) render(data.currentContent); else if (data && (data.title || data.kind || data.event)) render(data); if (data?.onDeckMap) onDeckMap = data.onDeckMap; if (data?.completionRatings) ratings = data.completionRatings; if (data?.settings) settings = data.settings; if (isVisible) render(); } };
}
